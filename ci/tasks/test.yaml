---
platform: linux

image_resource:
  type: docker-image
  source: {repository: node, tag: '8-alpine'}

inputs:
  - name: nodejsCode
  - name: dependency-cache

run:
  path: nodejsCode/scripts/test.sh